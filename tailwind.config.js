const {colors} = require('tailwindcss/defaultTheme');

module.exports = {
    theme: {
        extend: {},
        colors: {
            primary: colors.indigo,
            secondary: colors.pink,
            primaryInverse: colors.white,
            secondaryInverse: colors.white,
            black: colors.black,
            transparent: colors.transparent,
            white: colors.white,
            gray: colors.gray
        }
    },
    variants: {},
    plugins: [],
}
